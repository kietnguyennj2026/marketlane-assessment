# Demo mode works without credentials.
PAYMENT_PROVIDER=demo
APP_URL=http://localhost:4317

# Optional: use a dedicated Stripe sandbox, never a live secret key.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
